function questionNine() {

    //Write constructor function here
    function Accumulator(startingValue) {
        9 = startingValue; 
    }
    function read()  {}
    let accumulator = new Accumulator(1); //initial value 1

    accumulator.read(); // adds the user-entered value need to prompt user to enter value

    accumulator.read(); // adds the user-entered value refers to something within the accumulator

    console.log(accumulator.value); // shows the sum of these values
}